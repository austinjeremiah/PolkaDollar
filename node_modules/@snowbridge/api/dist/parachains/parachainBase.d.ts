import { ApiPromise } from "@polkadot/api";
import { Asset, AssetMap, ChainProperties, PNAMap, SubstrateAccount } from "@snowbridge/base-types";
export declare abstract class ParachainBase {
    #private;
    provider: ApiPromise;
    specName: string;
    specVersion: number;
    parachainId: number;
    constructor(provider: ApiPromise, parachainId: number, specName: string, specVersion: number);
    chainProperties(): Promise<ChainProperties>;
    getNativeAccount(account: string): Promise<SubstrateAccount>;
    getNativeBalance(account: string): Promise<bigint>;
    getNativeBalanceLocation(relativeTo: "here" | "sibling"): any;
    getTokenBalance(account: string, ethChainId: number, tokenAddress: string, asset?: Asset): Promise<bigint>;
    calculateXcmFee(destinationXcm: any, asset: any): Promise<bigint>;
    calculateDeliveryFeeInDOT(destParachainId: number, xcm: any): Promise<bigint>;
    calculateDeliveryFeeInNative(destParachainId: number, xcm: any): Promise<bigint>;
    dryRunXcm(originParaId: number, xcm: any, findForwardedDestination?: number): Promise<{
        success: boolean;
        errorMessage: any;
        forwardedDestination: import("@polkadot/types/types").ITuple<[import("@polkadot/types/interfaces").VersionedMultiLocation, import("@polkadot/types").Vec<import("@polkadot/types/interfaces").VersionedXcm>]> | undefined;
    }>;
    abstract getLocationBalance(location: any, account: string, pnaAssetId?: any): Promise<bigint>;
    abstract getDotBalance(account: string): Promise<bigint>;
    abstract getAssets(ethChainId: number, pnas: PNAMap): Promise<AssetMap>;
    abstract getXC20DOT(): string | undefined;
}
//# sourceMappingURL=parachainBase.d.ts.map