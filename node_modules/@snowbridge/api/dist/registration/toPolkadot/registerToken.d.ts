import { ApiPromise } from "@polkadot/api";
import { AssetRegistry } from "@snowbridge/base-types";
import { Connections, RegistrationInterface, RegistrationFee, TokenRegistration, RegistrationValidationResult } from "./registrationInterface";
import { Context } from "../../index";
import { AbstractProvider } from "ethers";
export declare class RegisterToken implements RegistrationInterface {
    getRegistrationFee(context: Context | {
        assetHub: ApiPromise;
        bridgeHub: ApiPromise;
    }, registry: AssetRegistry, relayerFee: bigint, options?: {
        paddFeeByPercentage?: bigint;
    }): Promise<RegistrationFee>;
    createRegistration(context: Context | {
        ethereum: AbstractProvider;
    }, registry: AssetRegistry, sourceAccount: string, tokenAddress: string, fee: RegistrationFee): Promise<TokenRegistration>;
    validateRegistration(context: Context | Connections, registration: TokenRegistration): Promise<RegistrationValidationResult>;
}
//# sourceMappingURL=registerToken.d.ts.map