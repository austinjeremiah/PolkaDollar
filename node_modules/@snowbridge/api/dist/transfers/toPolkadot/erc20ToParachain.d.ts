import { ApiPromise } from "@polkadot/api";
import { AssetRegistry } from "@snowbridge/base-types";
import { Connections, TransferInterface } from "./transferInterface";
import { IGatewayV2 as IGateway } from "@snowbridge/contract-types";
import { Context } from "../../index";
import { DeliveryFee, Transfer, ValidationResult } from "../../toPolkadotSnowbridgeV2";
import { AbstractProvider } from "ethers";
export declare class ERC20ToParachain implements TransferInterface {
    getDeliveryFee(context: Context | {
        gateway: IGateway;
        assetHub: ApiPromise;
        bridgeHub: ApiPromise;
        destination: ApiPromise;
    }, registry: AssetRegistry, tokenAddress: string, destinationParaId: number, options?: {
        paddFeeByPercentage?: bigint;
        feeAsset?: any;
        customXcm?: any[];
        overrideRelayerFee?: bigint;
    }): Promise<DeliveryFee>;
    createTransfer(context: Context | {
        ethereum: AbstractProvider;
        assetHub: ApiPromise;
        destination: ApiPromise;
    }, registry: AssetRegistry, destinationParaId: number, sourceAccount: string, beneficiaryAccount: string, tokenAddress: string, amount: bigint, fee: DeliveryFee, customXcm?: any[]): Promise<Transfer>;
    validateTransfer(context: Context | Connections, transfer: Transfer): Promise<ValidationResult>;
}
//# sourceMappingURL=erc20ToParachain.d.ts.map